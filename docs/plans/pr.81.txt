Выполни по шагам в терминале в корне проекта:

Шаг 1 — добавь в корневой .gitignore строки для лишних папок.
Открой .gitignore и добавь в конец файла:

# Дизайн-референсы (внутренние материалы, не для репозитория)
design_reference/
design_reference */
design_reference*/

Шаг 2 — проверь что теперь git видит только нужные файлы:

git status

Шаг 3 — покажи мне вывод git status перед тем как делать add.
Не делай git add пока я не подтвержу.