Контекст: Transkrib SmartCut AI
ВАЖНО: Только эти действия.

1. Покажи точный путь к файлу HowItWorks.tsx

2. Покажи строки 1-10 файла — убедись что
   там нет слов: Carousel, Swiper, useState, 
   currentIndex, translateX

3. Если всё ещё есть карусель — найди
   КАКОЙ компонент реально рендерится на
   странице "Как работает". 
   Возможно это не HowItWorks.tsx а другой файл.
   Проверь: App.tsx, HomePage.tsx, routes.
   Найди где рендерится секция с карточками шагов.

4. После нахождения правильного файла —
   заменить карусель на простой grid там.

Отчёт: покажи имя файла и строки 1-15.