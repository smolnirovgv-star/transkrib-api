cd C:\Users\Admin\OneDrive\Desktop\Cursor\Transkrib_SmartCut_AI
rebuild_installer.bat
```

> 💡 Используем `rebuild_installer.bat` а не `build_all.bat` — он пересобирает только Electron (3-5 мин), backend.exe уже актуален после последней полной сборки.

---

## 📋 После сборки — план тестирования:
```
1. Установка
   → запустить Transkrib-Setup-1.0.0-x64.exe
   → проверить что устанавливается без ошибок
   → антивирус не блокирует

2. Первый запуск
   → SetupWizard открывается
   → Trial запускается (7 дней)
   → Карточка инструкции показывается

3. Авторизация
   → Регистрация на реальный email
   → Вход с паролем
   → Сброс пароля (новый flow)

4. Обработка видео
   → Загрузить короткое видео (2-3 мин)
   → Дождаться результата
   → Проверить транскрипт, экспорт, превью

5. UI
   → Все 3 экрана (Главная/Как работает/Цены)
   → Смена темы и языка
   → Подвал — ссылки на документы