Контекст: Transkrib SmartCut AI

ЗАДАЧА — Исправить 2 бага после тестирования:

БАГ 1 — highlight_scorer.py строка 59
"unterminated string literal" — необработанные \ в строках.
Найди и исправь все пути Windows в этом файле.

БАГ 2 — UI не показывает результат после завершения задачи
После перезагрузки страницы completed задача не отображается.
В App.tsx при старте проверять GET /api/tasks/ —
если есть задача со state=completed →
автоматически показывать ResultGallery с результатом.