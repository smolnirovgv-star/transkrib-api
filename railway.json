{
  "build": {"builder": "NIXPACKS"},
  "deploy": {
    "startCommand": "cd backend && python -m uvicorn main_railway:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
